spring:
  config:
    activate:
      on-profile: test
  sql:
    init:
      schema-locations: sql/schema.sql
      encoding: UTF-8
  datasource:
    url: jdbc:h2:mem:test/test
    username: user
    password:
    driver-class-name: org.h2.Driver
logging:
  level:
    sql: trace
