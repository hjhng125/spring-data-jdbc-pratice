server:
  port: 9998

spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/test?rewriteBatchedStatements=true&profileSQL=true&logger=Slf4jLogger&maxQuerySizeToLog=999999
    username: user
    password: pass
logging:
  level:
    sql: trace